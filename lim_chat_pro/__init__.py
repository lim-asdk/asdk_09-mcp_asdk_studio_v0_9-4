import os
import sys

# MCP ASDK Studio v1 Package Initialization
# This package contains the core engine and UI for the general-purpose studio.

PRO_ROOT = os.path.dirname(os.path.abspath(__file__))
ENGINE_PATH = os.path.join(PRO_ROOT, "engine")

# Note: IQ-Packs are no longer supported in this version to prioritize general studio usage.
